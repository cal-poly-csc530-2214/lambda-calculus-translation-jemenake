
Author: Joe Emenaker
Written in: Haskell (GHC)
Target Langs: Python and Javascript

Build with 'ghc --make main'

In main.hs, you can choose what output you want by adjusting the comments
on the emmitters:

    input <- getLine
    -- emitLC input
    -- emitJS input
    emitPy input

Emits the original lambda calculus syntax, JS, or Python

